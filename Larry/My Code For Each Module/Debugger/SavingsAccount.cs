using System;
/*
namespace Classes
{
    internal class SavingsAccount : BankAccount
    {
        static decimal defaultInterestRate = 0.05m;
        decimal interestRate = defaultInterestRate;
        /*
        public SavingsAccount()
        {
        }
        
        public SavingsAccount(string name, decimal initialBalance): base(name, initialBalance)
        {
            Console.WriteLine($"{name} now has a savings account with balance of {initialBalance:c}");
        }

        public SavingsAccount(string name, decimal initialBalance, decimal interestRate):this(name, initialBalance)
        {
            this.interestRate = interestRate;
            Console.WriteLine($"""
             {name} now has a savings account with a balance
             $"{initialBalance:c} and non-default"); 
             """);
        }
    }

}
*/